name: Build and Deploy

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.24"

      - name: Build application
        run: |
          GOOS=linux GOARCH=arm64 go build -o bluegreen cmd/main.go

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-binary
          path: bluegreen

  deploy:
    needs: build
    runs-on: [self-hosted, bluegreen-localvm]

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: app-binary
          path: /tmp

      - name: Stop existing application (if running)
        run: pkill -f "bluegreen" || true

      - name: Deploy binary
        run: |
          mkdir -p ~/app
          cp /tmp/bluegreen ~/app/bluegreen
          chmod +x ~/app/bluegreen
      - name: Start application
        run: |
          cd ~/app
          # Start the process in a way that survives the runner session
          setsid ./bluegreen </dev/null >/dev/null 2>&1 &
          disown
          echo "Application started"

      - name: Verify deployment
        run: |
          sleep 2
          curl -f http://localhost:8080/healthz || exit 1
          echo "Deployment verified successfully"
